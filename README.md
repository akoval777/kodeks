# Тестовое задание (React.js)
Одностраничное приложение для работы с базой автомобилей.

1. Установка зависимостей
`npm install`
2. Сборка и запуск сервера
`npm run start`
3. Приложение будет доступно по адресу http://localhost:8080
